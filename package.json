{
  "name": "lorefit",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"node backend/index.js\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend run migrate && node backend/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
