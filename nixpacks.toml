[phases.install]
cmds = [
  "npm --prefix backend install",
  "npm --prefix frontend install"
]

[phases.build]
cmds = ["npm --prefix frontend run build"]

[start]
cmd = "npm --prefix backend run migrate && node backend/index.js"
